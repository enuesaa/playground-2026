generator client {
  provider = "prisma-client"
  output   = "../src/genprisma"
  runtime  = "workerd"
}

datasource db {
  provider = "sqlite"
}

model Note {
  id    String  @id  @default(uuid())
  title String  @unique
  desc  String?
}
