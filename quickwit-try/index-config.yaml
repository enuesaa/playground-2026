version: 0.8

index_id: logs

doc_mapping:
  field_mappings:
    - name: timestamp
      type: datetime
      input_formats:
        - rfc3339

    - name: level
      type: text
      tokenizer: raw

    - name: message
      type: text

search_settings:
  default_search_fields:
    - message
