services:
  neo4j:
    image: neo4j:latest
    ports:
      - 7474:7474 # Admin
      - 7687:7687 # Bolt
    environment:
      - NEO4J_AUTH=neo4j/password
    volumes:
      - neo4jdata:/data

volumes:
  neo4jdata:
