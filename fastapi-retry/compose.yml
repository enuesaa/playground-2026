services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 8000:80
    volumes:
      - .:/workspace
    command: uv run uvicorn app.main:app --host 0.0.0.0 --port 80 --reload
