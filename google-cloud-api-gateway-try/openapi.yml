swagger: "2.0"

info:
  title: My API
  version: "1.0.0"

schemes:
  - https

paths:
  /health:
    get:
      summary: Health check
      responses:
        "200":
          description: OK
          schema:
            type: object
            properties:
              status:
                type: string

definitions: {}
